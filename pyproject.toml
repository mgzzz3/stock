[project]
name = "stock"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "tushare>=1.4",
    "pandas>=2.2",
    "python-dotenv>=1.0",
    "truststore>=0.10",
    "tqdm>=4.66",
    "akshare>=1.18.60",
]

[tool.uv]
package = false
